## GIT ##
REPOSITORIOS:
https://github.com/jonathantorralba/Larave7Livewire2.0

COMANDOS
git init

git add
git add . -> todo el directorio
git commit

git commit -am "Descripcion"

git commit --amend -> cambiar descripcion del ultimo commmit via VIM

git config --global user.username "nombre"
git config --global user.email "correo@correo.es"

git reset --hard (codigo del commit)

git status -s
git log --oneline


git pull -> de Github a nuestro PC

git push -> PC to Github
git push --tags
git push --force

git branch
git merge xxxx ->desde rama master

